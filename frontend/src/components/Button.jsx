import React from 'react'

const Button = ({text}) => {
  
  return (
    <button className="text-white bg-black rounded-md border-none px-4 py-2 font-bold">{text}</button>
  )
}

export default Button